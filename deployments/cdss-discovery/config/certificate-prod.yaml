apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  annotations:
  name: tls-cert
spec:
  dnsNames:
  - cdss-discovery.datahub.berkeley.edu
  issuerRef:
    kind: Issuer
    name: letsencrypt
  secretName: tls-cert
