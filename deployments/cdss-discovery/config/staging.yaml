jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    hosts:
      - cdss-discovery-staging.datahub.berkeley.edu
    tls:
      - secretName: tls-cert
        hosts:
          - cdss-discovery-staging.datahub.berkeley.edu
